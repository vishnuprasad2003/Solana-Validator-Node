# ─────────────────────────────────────────────────────────────────────────────
# Solana Validator — Validator-1
# ─────────────────────────────────────────────────────────────────────────────

# ─── BASE STORAGE ────────────────────────────────────────────────────────────
BASE_DIR="/solana"

# ─── NODE IDENTITY ───────────────────────────────────────────────────────────
NODE_NAME="validator-1"
NODE_TYPE="validator"

# ─── NETWORK PORTS ───────────────────────────────────────────────────────────
GOSSIP_PORT=8026
RPC_PORT=8901
RPC_BIND_ADDRESS="0.0.0.0"
DYNAMIC_PORT_RANGE_START=8030
DYNAMIC_PORT_RANGE_END=8055

# ─── GENESIS ─────────────────────────────────────────────────────────────────
GENESIS_LAMPORTS=1000000000000000000
BOOTSTRAP_STAKE_LAMPORTS=1000000000000
CLUSTER_TYPE="development"

# ─── CLUSTER CONNECTION ─────────────────────────────────────────────────────
ENTRYPOINT="localhost:8001"
KNOWN_VALIDATOR="9R5uF5F35jhzzbwvr45GSE5Uu9DAWiTE3fmsP7XGDkp5"
EXPECTED_GENESIS_HASH="BgCCzadnyvZeuoxEDTDLwfqrd9P4oGFcaqk9iA2CyZzY"

# ─── PERFORMANCE / STORAGE ──────────────────────────────────────────────────
LIMIT_LEDGER_SIZE=50000000

# ─── DERIVED PATHS ───────────────────────────────────────────────────────────
IDENTITY_KEY="${BASE_DIR}/keys/${NODE_NAME}-identity.json"
VOTE_KEY="${BASE_DIR}/keys/${NODE_NAME}-vote.json"
STAKE_KEY="${BASE_DIR}/keys/${NODE_NAME}-stake.json"
FAUCET_KEY="${BASE_DIR}/keys/faucet.json"
LEDGER_DIR="${BASE_DIR}/data/${NODE_NAME}"
LOG_FILE="${BASE_DIR}/logs/${NODE_NAME}.log"
PID_FILE="${BASE_DIR}/pids/${NODE_NAME}.pid"
PROGRAMS_DIR="${BASE_DIR}/programs"
